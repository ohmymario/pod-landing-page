import styled from "styled-components";

const AttrStyles = styled.footer`
   {
    /* font-family: var(--body-font); */
    font-size: 11px;
    text-align: center;
    letter-spacing: 0.5px;
    /* color: var(--white); */
    a {
      /* color: var(--white); */
      transition: all 0.35s;
    }
  }
`;

export default AttrStyles;
